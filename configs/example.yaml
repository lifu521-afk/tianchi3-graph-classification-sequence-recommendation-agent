seed: 20260722
output_dir: outputs

classification:
  alpha: 3.0
  smoothing_alpha: 0.65
  smoothing_clamp: 0.75
  smoothing_steps: 6

recommendation:
  topk: 10
  validation_repeats: 3
  history_buckets: [0, 1, 2, 3]
  note: "正式提交前必须使用与测试历史长度匹配的验证协议。"

